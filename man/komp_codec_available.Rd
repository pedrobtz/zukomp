% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codecs.R
\name{komp_codec_available}
\alias{komp_codec_available}
\title{Is a codec implementation available?}
\usage{
komp_codec_available(codec)
}
\arguments{
\item{codec}{A codec name, as listed in \code{\link[=komp_codecs]{komp_codecs()}}'s \code{id} column.}
}
\value{
\code{TRUE} if an implementation is registered, \code{FALSE} if the codec is
known to zukomp but not installed. An unknown name is an error of class
\code{zukomp_unsupported_codec}, since it is far more likely to be a typo
than a deliberate probe.
}
\description{
Is a codec implementation available?
}
\examples{
komp_codec_available("identity")

# a codec zukomp knows of, but which ships in a separate package
komp_codec_available("zstd")
}
